apiVersion: v1
kind: Service
metadata:
  name: {{ include "ml-inference-service.fullname" . }}
  labels:
    {{- include "ml-inference-service.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "ml-inference-service.selectorLabels" . | nindent 4 }}
