apiVersion: v1
kind: Service
metadata:
  name: demo-nginx-service
  namespace: application
  labels:
    app: demo-nginx
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: demo-nginx